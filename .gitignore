# Local Claude config / artifacts
.claude/*
# ...but share the curated skills, agents, and slash commands
!.claude/skills/
!.claude/commands/
!.claude/agents/

# Devcontainer config (ignore if you don't want it versioned)
.devcontainer/.env

# Node / JS
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
*.swp
